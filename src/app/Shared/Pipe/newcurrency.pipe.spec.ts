import { NewcurrencyPipe } from './newcurrency.pipe';

describe('NewcurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new NewcurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
